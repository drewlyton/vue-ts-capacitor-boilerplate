import
{{pascalCase name}}
from './{{pascalCase name}}.vue'; import { mount } from '@vue/test-utils';
describe('{{pascalCase name}}.vue', () => { it('Should render', () => {
expect(mount({{pascalCase name}}).html()).toBeTruthy(); }); });