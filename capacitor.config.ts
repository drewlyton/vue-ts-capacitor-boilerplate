import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.test.app',
  appName: 'vue-ts-mobile-template',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
